ARG NODE_IMAGE=m.daocloud.io/library/node:20-slim
FROM ${NODE_IMAGE}

WORKDIR /app/web_ui
COPY web_ui/package.json web_ui/package-lock.json ./
RUN npm install

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
